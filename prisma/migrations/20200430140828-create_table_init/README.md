# Migration `20200430140828-create_table_init`

This migration has been generated by antoniomussi at 4/30/2020, 2:08:28 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Comment" (
    "archived" boolean  NOT NULL DEFAULT false,
    "channelId" integer  NOT NULL ,
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" SERIAL,
    "nickname" text  NOT NULL ,
    "text" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Channel" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "id" SERIAL,
    "transmitting" boolean  NOT NULL DEFAULT false,
    "url" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

ALTER TABLE "public"."Comment" ADD FOREIGN KEY ("channelId")REFERENCES "public"."Channel"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200430140828-create_table_init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,29 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgres"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Comment {
+    id          Int @id @default(autoincrement())
+    text        String
+    nickname    String
+    channel     Channel @relation(fields: [channelId], references: [id])
+    createdAt   DateTime @default(now())
+    archived    Boolean @default(false)
+    channelId   Int
+}
+
+model Channel {
+    id              Int @id @default(autoincrement())
+    url             String
+    createdAt       DateTime @default(now())
+    transmitting    Boolean @default(false)
+    comments        Comment[]
+}
```


