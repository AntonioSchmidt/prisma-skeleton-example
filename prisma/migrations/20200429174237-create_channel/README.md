# Migration `20200429174237-create_channel`

This migration has been generated by antoniomussi at 4/29/2020, 5:42:37 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Channel" (
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "transmitting" BOOLEAN NOT NULL DEFAULT false ,
    "url" TEXT NOT NULL  
) 

CREATE TABLE "quaint"."new_Comment" (
    "archived" BOOLEAN NOT NULL DEFAULT false ,
    "channelId" INTEGER NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "nickname" TEXT NOT NULL  ,
    "text" TEXT NOT NULL  ,FOREIGN KEY ("channelId") REFERENCES "Channel"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Comment" ("archived", "createdAt", "id", "nickname", "text") SELECT "archived", "createdAt", "id", "nickname", "text" FROM "quaint"."Comment"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Comment";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Comment" RENAME TO "Comment";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200429115449-add-nickname-comment..20200429174237-create_channel
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider = "prisma-client-js"
@@ -13,8 +13,17 @@
 model Comment {
     id          Int @id @default(autoincrement())
     text        String
     nickname    String
-    streamId    String
+    channel     Channel @relation(fields: [channelId], references: [id])
     createdAt   DateTime @default(now())
     archived    Boolean @default(false)
+    channelId   Int
 }
+
+model Channel {
+    id              Int @id @default(autoincrement())
+    url             String
+    createdAt       DateTime @default(now())
+    transmitting    Boolean @default(false)
+    comments        Comment[]
+}
```


